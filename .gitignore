# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# Misc cache
.cache

# Playwright
test-results*
playwright-report
# 临时 E2E 输出目录（安全删除受限时保留在本地，不入库）
.tmp-e2e*
blob-report

# Proxy runtime artifacts
proxy-logs/
proxy-data/

# Visual regression snapshots (baseline kept in repo; local updates ignored)
e2e/visual.spec.ts-snapshots/*.png

# CodeBuddy local project data (not for repo)
.codebuddy/

# WorkBuddy local project data (not for repo)
.workbuddy/

# Promo film: only engineering sources are versioned (film.html / film.js / shoot.cjs / compose_bgm.py);
# rendered mp4 / wav go to Git LFS or Release assets — see doc/v0.9.3-roadmap.md (P2-C)
video/*.mp4
video/*.wav
video/frames/

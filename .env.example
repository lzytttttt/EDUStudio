# 轻后端代理接入（可选）
#
# 本地开发：复制本文件为 .env.local 后填写（.env.local 不入版本库）。
# 静态托管：在 Cloudflare Pages / Vercel 控制台的「环境变量」中配置同名变量，二者等价。
# 注意：Vite 变量在**构建期**注入，改动后需重新构建 / Retrigger deployment。

# 代理地址：非空时前端首次打开即为代理模式（key 由代理保管，浏览器不持有），
# 用户无需在设置页手填；留空则沿用直连模式（需用户自备 key）。
# 形如 https://edustudio-proxy.<account>.workers.dev/v1
VITE_PROXY_URL=

# 远端数据源地址覆盖（可选）：缺省时由 VITE_PROXY_URL 推导为 <proxy>/api/sources；
# 未配置代理时回落 http://localhost:8787/api/sources。
VITE_SOURCE_URL=
